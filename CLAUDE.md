@AGENTS.md

For UI aesthetic requests, use the `design-style-director` skill first to classify the target style family before coding. If the family is already explicit, use the matching `family-*` skill directly.
